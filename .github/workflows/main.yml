name: Drift Scanning

on:
  push:
    branches:
      - dev

env:
  PROJECT_NUMBER: 987121777525

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    steps:
      - name: Checkout
        uses: 'actions/checkout@v4'

